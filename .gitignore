# ---- 안드로이드 앱(app/, Capacitor) ----
app/node_modules/

# index.html + vendor 를 복사/번들해서 매번 다시 만들어지는 폴더 (npm run prepare:www)
app/www/

# Gradle / Android Studio 빌드 산출물 및 로컬 설정
app/android/.gradle/
app/android/build/
app/android/app/build/
app/android/capacitor-cordova-android-plugins/build/
app/android/local.properties
app/android/captures/
app/android/*.iml
app/android/.idea/

# 서명 키/비밀번호 — 절대 커밋 금지 (분실 시 이 키로 낸 앱은 다시는 업데이트 불가)
app/keystore/
app/keystore-passwords.tmp
app/android/keystore.properties
app/android/app/*.jks
app/android/app/*.keystore

*.log
